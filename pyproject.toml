[project]
name = "inventory-sales"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "Inventory_Sales.settings"
python_files = ["test_*.py", "*_test.py"]
addopts = ["--ds=Inventory_Sales.settings", "--tb=short"]
